# STAT 418 — Chapter 6 runnable scripts
# Purdue GenAI Studio wrapper (Dr. Reese's SDK), pinned:
genai-studio-sdk @ git+https://github.com/treese41528/genai-studio-sdk.git@v1.2.1

# The SDK needs the openai 1.x client (`from openai import OpenAI`); v1.2.1's
# metadata already requires openai>=1.0 — stated here too as belt-and-suspenders.
openai>=1.0

# Used by later sections (embeddings, annotation, evaluation):
numpy
pandas
scikit-learn
matplotlib

# Optional — only for ch6_2_embeddings/06_concept_arithmetic.py, the static
# Word2Vec/GloVe control. Not needed for any other script; uncomment to run it:
# gensim>=4.3
